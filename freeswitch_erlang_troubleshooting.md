Can't answer a call, because messages that require the call UUID fail with `nosession` or `-ERR No such channel!`. Just learned that there is an `fs_cli` command called

```
erlang debug on
```

but I couldn't discern any difference in the log output.
