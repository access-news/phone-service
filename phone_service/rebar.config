{erl_opts, [debug_info]}.
{deps, []}.

{ relx
, [ {release
    , { phone_service, "0.1.0" }
    , [ phone_service
      , content_hub  % extract to it's own service
      % implement when `content_hub` is out
    % , content_cache
    % , sasl
      ]
    }
  , {sys_config, "./config/sys.config"}
  , {vm_args, "./config/vm.args"}
  , {dev_mode, true}
  , {include_erts, false}
  , {extended_start_script, true}
  ]
}.

{ profiles
, [ { prod
    , [ { relx
        , [ {dev_mode, false}
          , {include_erts, true}
          ]
        }
      ]
    }
  ]
}.
